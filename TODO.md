# TODO

## 優先度高

- `capture_region` の調整を楽にする。現在は数値を手で編集する必要があり、初期設定の負荷が高い。
- テンプレート収集の導線を改善する。最低限、保存済みテンプレートの一覧確認と削除は欲しい。
- 認識失敗時のデバッグ情報を増やす。現在は `runtime_state.json` だけでは原因切り分けが弱い。

## 優先度中

- OCR を併用して、テンプレートマッチだけに依存しない認識経路を追加する。
- 画像前処理を増やして、解像度差や UI スケール差に強くする。
- 対戦フェーズごとに認識領域を切り替えられるようにする。
- OBS ブラウザソース向けの表示調整を追加する。

## 優先度低

- Windows ネイティブ版と WSL 版の起動スクリプトを整理して、用途別にまとめる。
- デスクトップオーバーレイにクリック透過の切り替えを追加する。
- EXE 化手順を自動化して配布しやすくする。
